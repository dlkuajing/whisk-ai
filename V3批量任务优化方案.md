# V3 批量任务优化方案

**分析日期**：2025-10-21
**问题来源**：用户反馈批量任务窗口队列模式使用问题

---

## 问题分析

### 当前问题

从截图和用户反馈分析出以下问题：

#### 问题1：无法在过程中添加任务 ❌

**V3当前流程**：
```
1. 输入批量提示词
2. 点击"生成批量任务" → 创建batch_tasks列表
3. 点击"启动队列" → 将所有任务一次性添加到窗口队列
4. 执行过程中无法再添加新任务
```

**V2参考流程**：
```
1. 输入提示词
2. 点击"添加任务" → 直接添加到窗口队列
3. 继续输入新提示词
4. 再次点击"添加任务" → 继续添加到队列
5. 可以随时添加，不需要"先生成再启动"
```

**根本原因**：
- V3的批量任务设计成了"先生成列表，再批量启动"的模式
- 一旦点击"启动队列"，batch_tasks就被锁定
- 没有提供在执行过程中动态添加新任务的机制

#### 问题2：任务进度状态显示错误 ❌

**截图显示**：
```
窗口队列状态（批量任务）
浏览器：1
队列状态：运行中
待处理任务：N/A        ← 应该显示数字，显示N/A说明数据获取失败
生成进度：1

批量任务列表状态：
□ B001: 待执行 - 0/2   ← 应该显示"执行中"或"已完成"
□ B002: 待执行 - 0/2   ← 状态没有更新
```

**根本原因**：
- 窗口队列回调函数的签名不匹配
- V2: `handle_queue_callback(event_type, browser_id, data)`
- V3: `handle_queue_callback_batch(event_type, data)` ← 缺少browser_id参数
- 导致回调函数无法正确获取和更新任务状态

#### 问题3：信息架构混乱 ❌

**V3当前架构**：
```
批量任务标签页
├─ 左侧：批量导入（多个提示词）
├─ 右侧面板1：批量任务队列预览（传统模式）
└─ 右侧面板2：窗口队列状态（队列模式）
   └─ 显示：窗口队列信息 + 批量任务列表状态
```

**问题**：
- 两套数据源（batch_tasks 和 window_queue）
- 状态同步复杂，容易出错
- 用户需要理解"批量任务列表"和"窗口队列"的区别

---

## V2成功经验

### V2的设计特点

#### 1. 任务直接添加到窗口队列
```python
def add_task_to_queue(browser_id, browser_display, prompt):
    # 生成任务ID
    task_id = f"Q{self.thread_counter:03d}"

    # 准备任务参数
    task_params = {...}

    # 直接添加到窗口队列
    self.window_queue_manager.add_task_to_window(
        browser_id, browser_display, task_params, session_mode, session_name
    )

    # 在TreeView中显示
    tree_item = self.task_tree.insert('', 'end', ...)
    self.queue_task_items[task_id] = {'tree_item': tree_item, ...}
```

**优点**：
- ✅ 无需"先生成再启动"
- ✅ 可以随时添加新任务
- ✅ 单一数据源（窗口队列）
- ✅ 状态同步简单

#### 2. 统一的回调机制
```python
def handle_queue_callback(self, event_type, browser_id, data):
    """处理窗口队列的回调事件"""
    if event_type == 'task_start':
        task_id = data.get('task_id', '')
        # 更新TreeView状态
        self.message_queue.put(('queue_task_status', task_id,
                               ('进度', '执行中', '状态', '运行中')))

    elif event_type == 'task_progress':
        task_id = data['task']['task_id']
        current = data['current']
        total = data['total']
        progress_text = f"生成:{current}/{total}"
        self.message_queue.put(('queue_task_progress', task_id, progress_text))

    elif event_type == 'task_complete':
        task_id = data.get('task_id', '')
        self.message_queue.put(('queue_task_status', task_id,
                               ('进度', '已完成', '状态', '已完成')))
```

**优点**：
- ✅ 包含browser_id参数，便于多浏览器管理
- ✅ 通过message_queue进行线程安全的GUI更新
- ✅ 支持详细的进度类型（generation/download）
- ✅ 事件类型全面（task_added/start/complete/failed/progress/error）

#### 3. TreeView即时显示
```python
# 添加任务时立即创建TreeView项
tree_item = self.task_tree.insert('', 'end', text=task_id, values=(
    browser_display.split(' ')[0],
    prompt[:30] + "...",
    ratio,
    count,
    "排队中",    # 初始状态
    "等待中"     # 初始进度
))

# 回调更新时修改TreeView
self.message_queue.put(('queue_task_status', task_id,
                       ('进度', '执行中', '状态', '运行中')))
```

**优点**：
- ✅ 立即反馈，用户体验好
- ✅ 单一界面显示所有任务
- ✅ 状态和进度实时更新

---

## 优化方案

### 方案A：快速任务模式增强（推荐）⭐

**核心思想**：将快速任务标签页的"添加任务"功能增强，支持批量快速添加

#### 新增功能：批量快速添加模式

**左侧配置区修改**：
```
┌─────────────────────────────────┐
│ 💡 快速开始                      │
├─────────────────────────────────┤
│ 任务名称: [任务_141540]          │
│                                 │
│ 浏览器: [1 (运行中)] [🔄]       │
│                                 │
│ 提示词模式:                     │
│ ○ 单个提示词                    │
│ ● 批量提示词（每行一个）✨       │
│                                 │
│ 提示词: [文本框]                │
│ (批量模式下显示多行文本框)       │
│                                 │
│ 纵横比: [1:1] [4:3] [3:4]...    │
│ 生成数量: [━━●━━] 4轮           │
│                                 │
│ 🔧 高级选项 (可折叠)            │
│                                 │
│ 📋 执行模式                     │
│ ☑️ 启用窗口队列模式             │
│ 归档模式: ● 创建新任务组        │
│ 任务组名称: [套图_01]          │
│                                 │
│ [➕ 批量添加任务]  ✨           │
│ (批量模式下显示这个按钮)         │
└─────────────────────────────────┘
```

**实现逻辑**：
```python
def add_batch_tasks_quick(self):
    """快速批量添加任务"""
    # 获取批量提示词
    prompts_text = self.prompt_textbox.get("1.0", "end-1c").strip()
    prompts = [p.strip() for p in prompts_text.split('\n') if p.strip()]

    if not prompts:
        messagebox.showerror("错误", "请输入提示词")
        return

    # 逐个添加到窗口队列
    for idx, prompt in enumerate(prompts, 1):
        # 调用现有的add_task_to_queue_quick方法
        self.add_single_task_to_queue(prompt, f"批量任务{idx}")

    messagebox.showinfo("成功", f"已添加 {len(prompts)} 个任务到队列")

def add_single_task_to_queue(self, prompt, task_name):
    """添加单个任务到队列（内部方法）"""
    # 生成任务ID
    self.thread_counter += 1
    task_id = f"Q{self.thread_counter:03d}"

    # 准备任务参数
    task_params = {
        'task_id': task_id,
        'task_name': task_name,
        'prompt': prompt,
        'count': self.count_var.get(),
        'aspect_ratio': self.ratio_var.get(),
        'base_save_dir': self.save_dir_var.get()
    }

    # 获取归档模式
    archive_mode = self.quick_archive_mode.get()
    session_name = self.quick_session_name_entry.get().strip() if archive_mode == 'new_session' else None

    # 添加到窗口队列
    browser_id = self.browser_id_map[self.browser_var.get()]
    browser_display = self.browser_var.get()

    self.window_queue_manager.add_task_to_window(
        browser_id, browser_display, task_params, archive_mode, session_name
    )

    # 在TreeView中创建项（如果是任务列表面板）
    if hasattr(self, 'quick_current_panel') and self.quick_current_panel == "task_list":
        tree_item = self.task_tree.insert('', 'end', text=task_id, values=(...))
        self.queue_task_items[task_id] = {'tree_item': tree_item, ...}
```

**优点**：
- ✅ 复用现有的窗口队列机制
- ✅ 可以随时添加新任务
- ✅ 统一的状态更新
- ✅ 代码改动最小
- ✅ 用户学习成本低

**缺点**：
- ⚠️ 批量任务标签页功能重复

---

### 方案B：批量任务标签页重构（彻底）

**核心思想**：将批量任务标签页改造成"批量快速添加"模式

#### 重构内容

**1. 移除"生成批量任务"和"启动队列"的两步流程**

**修改前**：
```
1. 输入批量提示词
2. 点击"生成批量任务" → 创建batch_tasks列表
3. 预览批量任务列表
4. 点击"启动队列" → 批量添加到窗口队列
```

**修改后**：
```
1. 输入批量提示词
2. 点击"批量添加到队列" → 直接逐个添加到窗口队列
3. 右侧实时显示队列状态（类似V2）
```

**2. 修改回调函数签名**

**当前代码（有问题）**：
```python
# 初始化窗口队列管理器
self.window_queue_manager = WindowQueueManager(
    gui_callback=self.handle_queue_callback_batch
)

def handle_queue_callback_batch(self, event_type, data):
    # ❌ 缺少browser_id参数
    task_id = data.get('task_id', '')
    # ...
```

**修改为**：
```python
# 初始化窗口队列管理器
self.window_queue_manager = WindowQueueManager(
    gui_callback=self.handle_queue_callback_batch
)

def handle_queue_callback_batch(self, event_type, browser_id, data):
    # ✅ 添加browser_id参数，与V2保持一致
    task_id = data.get('task_id', '')

    # 通过message_queue更新GUI（线程安全）
    if event_type == 'task_start':
        self.message_queue.put(('log', browser_id, f"开始执行批量任务: {task_id}"))
        self.message_queue.put(('batch_task_status', task_id, '执行中'))

    elif event_type == 'task_progress':
        current = data.get('current', 0)
        total = data.get('total', 0)
        self.message_queue.put(('batch_task_progress', task_id, f"{current}/{total}"))

    elif event_type == 'task_complete':
        downloads = data.get('downloads', 0)
        self.message_queue.put(('log', browser_id, f"批量任务完成: {task_id}"))
        self.message_queue.put(('batch_task_status', task_id, f'已完成 - {downloads}张'))
```

**3. 右侧面板简化**

**移除**：
- ❌ 批量任务队列预览面板（不再需要batch_tasks列表）
- ❌ 双面板切换机制

**保留并增强**：
- ✅ 窗口队列状态面板
- ✅ 实时显示所有任务状态（类似V2的TreeView）

**新界面结构**：
```
┌─────────────────────────────────────────┐
│ 批量任务标签页                           │
├────────────┬────────────────────────────┤
│ 左侧配置   │ 右侧：任务队列状态          │
│            │                            │
│ 批量提示词 │ 📊 窗口队列 - 1 (运行中)   │
│ [多行文本] │                            │
│            │ 🔄 执行中: Q001            │
│ 浏览器     │ ⏳ 排队中: Q002, Q003     │
│ 比例/数量  │ ✅ 已完成: 3个任务         │
│            │                            │
│ 执行模式   │ 📋 任务详情:               │
│ ☑️ 窗口队列 │ ID   提示词      进度  状态│
│ 归档模式   │ Q001 prompt1... 3/4   执行中│
│            │ Q002 prompt2... 0/4   排队中│
│ [批量添加] │ Q003 prompt3... 0/4   排队中│
│            │ Q004 prompt4... 4/4   已完成│
│            │                            │
│            │ [🔄 刷新状态]              │
└────────────┴────────────────────────────┘
```

**优点**：
- ✅ 彻底解决"无法在过程中添加任务"的问题
- ✅ 统一数据源（窗口队列）
- ✅ 状态更新准确
- ✅ 界面更简洁

**缺点**：
- ⚠️ 需要较大代码改动
- ⚠️ 丢失批量任务预览功能

---

### 方案C：混合方案（平衡）⭐⭐

**核心思想**：保留批量任务列表的预览功能，但允许动态添加

#### 实现方式

**1. 修改"启动队列"行为**

**当前**：
```python
def start_batch_queue(self):
    # 一次性将所有batch_tasks添加到窗口队列
    for task in self.batch_tasks:
        self.window_queue_manager.add_task_to_window(...)

    # 清空batch_tasks或锁定
```

**修改为**：
```python
def start_batch_queue(self):
    # 标记为"启动模式"，但不锁定
    self.batch_queue_running = True

    # 逐个添加到窗口队列
    for task in self.batch_tasks:
        if task['status'] == '待执行':  # 只添加未执行的
            self.window_queue_manager.add_task_to_window(...)
            task['status'] = '队列中'
```

**2. 允许动态添加新任务**

**新增按钮**：
```
[生成批量任务]  [启动队列]  [继续添加] ✨
```

**实现**：
```python
def continue_add_batch_tasks(self):
    """继续添加新的批量任务"""
    # 获取新的提示词
    prompts_text = self.batch_prompt_text.get("1.0", "end-1c").strip()
    prompts = [p.strip() for p in prompts_text.split('\n') if p.strip()]

    # 生成新任务
    for idx, prompt in enumerate(prompts, len(self.batch_tasks) + 1):
        task = {
            'id': f"B{idx:03d}",
            'prompt': prompt,
            'status': '待执行',
            ...
        }
        self.batch_tasks.append(task)

    # 如果队列正在运行，立即添加新任务到窗口队列
    if self.batch_queue_running:
        for task in self.batch_tasks:
            if task['status'] == '待执行':
                self.window_queue_manager.add_task_to_window(...)
                task['status'] = '队列中'

    # 更新显示
    self.update_batch_queue_display()
```

**3. 修复回调函数签名**

```python
# 修改回调函数签名以匹配V2
def handle_queue_callback_batch(self, event_type, browser_id, data):
    """处理窗口队列的回调（批量任务）"""
    task_id = data.get('task_id', '')

    # 在批量任务列表中查找对应任务
    for task in self.batch_tasks:
        if task['id'] == task_id:
            if event_type == 'task_start':
                task['status'] = '执行中'
                self.log_message(f"🔄 批量任务 {task_id} 开始执行")

            elif event_type == 'task_progress':
                current = data.get('current', 0)
                total = data.get('total', 0)
                task['progress'] = f"{current}/{total}"
                self.log_message(f"📊 {task_id}: {task['progress']}")

            elif event_type == 'task_complete':
                task['status'] = '已完成'
                downloads = data.get('downloads', 0)
                task['progress'] = f'{downloads}张'
                self.log_message(f"✅ 批量任务 {task_id} 完成")

            # 更新批量任务列表显示
            self.update_batch_queue_display()

            # 更新窗口队列状态显示
            if self.batch_current_panel == "window_queue_status":
                self.refresh_batch_window_queue_status()

            break
```

**优点**：
- ✅ 保留批量任务预览功能
- ✅ 允许在过程中继续添加任务
- ✅ 修复回调函数签名问题
- ✅ 代码改动适中

**缺点**：
- ⚠️ 仍然有两套数据源
- ⚠️ 逻辑稍复杂

---

## 推荐方案

### 短期修复（立即实施）✅

**修复回调函数签名问题**

**步骤1**：检查`WindowQueueManager`的回调调用方式
```python
# 在whisk_window_queue.py中查找回调调用
if self.gui_callback:
    self.gui_callback(event_type, data)  # ❌ 缺少browser_id
```

**修改为**：
```python
if self.gui_callback:
    self.gui_callback(event_type, self.browser_id, data)  # ✅ 添加browser_id
```

**步骤2**：修改V3的回调函数签名
```python
# 快速任务
def handle_queue_callback_quick(self, event_type, browser_id, data):
    # ✅ 添加browser_id参数
    ...

# 批量任务
def handle_queue_callback_batch(self, event_type, browser_id, data):
    # ✅ 添加browser_id参数
    ...
```

**步骤3**：增强进度更新逻辑
```python
def handle_queue_callback_batch(self, event_type, browser_id, data):
    task_id = data.get('task_id', '')

    for task in self.batch_tasks:
        if task['id'] == task_id:
            if event_type == 'task_progress':
                # 从data中提取进度信息
                current = data.get('current', 0)
                total = data.get('total', 0)
                progress_type = data.get('progress_type', 'unknown')

                # 格式化进度文本
                if progress_type == 'generation':
                    task['progress'] = f"生成:{current}/{total}"
                elif progress_type == 'download':
                    task['progress'] = f"下载:{current}/{total}"
                else:
                    task['progress'] = f"{current}/{total}"

            # ... 其他事件处理 ...

            self.update_batch_queue_display()
            if self.batch_current_panel == "window_queue_status":
                self.refresh_batch_window_queue_status()
            break
```

### 中期优化（下一版本）🔮

**实施方案A或方案C**

**方案选择建议**：
- 如果希望快速解决问题，保持现有架构 → 选择**方案C（混合方案）**
- 如果希望彻底简化，提升用户体验 → 选择**方案A（快速任务增强）**

---

## 实施计划

### Phase 1：短期修复（立即）

- [ ] 修复`WindowQueueManager`回调函数签名
- [ ] 修改V3所有回调函数签名
- [ ] 增强进度更新逻辑
- [ ] 修复窗口队列状态显示的"N/A"问题
- [ ] 测试验证

**预计时间**：1-2小时
**优先级**：P0（高）

### Phase 2：中期优化（下一版本）

**选项1：方案C实施**
- [ ] 添加"继续添加"按钮
- [ ] 修改"启动队列"逻辑，不锁定batch_tasks
- [ ] 实现动态添加功能
- [ ] 测试验证

**选项2：方案A实施**
- [ ] 快速任务添加批量模式切换
- [ ] 实现批量快速添加逻辑
- [ ] 考虑移除或简化批量任务标签页

**预计时间**：3-5小时
**优先级**：P1（中）

---

## 附录：V2与V3对比

| 特性 | V2 | V3当前 | V3优化后 |
|-----|----|----|---------|
| **随时添加任务** | ✅ | ❌ | ✅ |
| **回调函数签名** | `(event, browser_id, data)` | `(event, data)` | `(event, browser_id, data)` |
| **进度显示** | ✅ 准确 | ❌ N/A | ✅ 准确 |
| **数据源** | 单一（窗口队列） | 双重（batch_tasks + 窗口队列） | 单一或简化 |
| **用户体验** | ✅ 流畅 | ❌ 复杂 | ✅ 流畅 |

---

**优化方案文档版本**：V1.0
**创建日期**：2025-10-21
**作者**：Claude Code (Sonnet 4.5)
