# WhiskAI V3 界面安装和使用说明

## 版本信息

- **版本**: V3.0.0
- **更新日期**: 2025-01-21
- **主要特性**: 基于CustomTkinter的现代化界面

---

## 新特性一览

### 🎨 视觉升级

- ✅ 现代化的界面设计
- ✅ 统一的颜色系统
- ✅ 深色/浅色主题切换
- ✅ 优雅的控件样式
- ✅ 流畅的交互动画

### 📑 标签页架构

- ✅ **快速任务** - 快速创建单个任务
- ✅ **窗口队列** - V2.1核心功能，窗口级队列管理
- ✅ **批量队列** - 批量任务管理

### 🎯 交互优化

- ✅ 图形化纵横比选择器（点击图标选择）
- ✅ 折叠式高级选项（默认隐藏，点击展开）
- ✅ 滑块式数量选择（实时预览）
- ✅ 增强的日志显示（Emoji标识）

---

## 安装步骤

### 方法1：一键启动（推荐）

直接双击运行 `启动V3界面.bat`，脚本会自动：
1. 检查Python环境
2. 安装缺失的依赖（CustomTkinter）
3. 启动WhiskAI V3界面

### 方法2：手动安装

#### 1. 安装依赖

```bash
# 安装CustomTkinter
pip install customtkinter>=5.2.0

# 或者使用requirements文件（推荐）
pip install -r requirements_v2.txt
```

#### 2. 启动程序

```bash
python whisk_gui_v3.py
```

---

## 界面使用指南

### 快速任务标签页

**适用场景**：创建单个或少量任务

**操作流程**：

1. **选择浏览器** - 点击刷新按钮获取运行中的浏览器
2. **输入提示词** - 在提示词框中输入图像描述
3. **选择纵横比** - 点击图形化按钮选择（正方形/横向/纵向/宽屏/竖屏）
4. **设置生成数量** - 拖动滑块或输入数字
5. **选择保存目录** - 默认为 `./downloads`
6. **（可选）高级选项** - 点击展开，设置延时、并发等
7. **点击"添加任务"** - 任务开始执行

**界面布局**：

```
┌─────────────────────┬────────────────────────┐
│  左侧：任务配置     │  右侧：任务管理        │
│  ━━━━━━━━━━━━━━━━━ │  ━━━━━━━━━━━━━━━━━━  │
│  💡 快速开始        │  📋 任务管理            │
│  · 任务名称         │  · 任务列表            │
│  · 浏览器选择       │  · 进度显示            │
│  · 提示词输入       │  · 任务控制            │
│  · 纵横比选择       │                        │
│  · 生成数量         │  📝 运行日志            │
│  · 保存目录         │  · 实时日志输出        │
│  ▶ 高级选项（折叠） │                        │
│  ━━━━━━━━━━━━━━━━━ │                        │
│  ➕ 添加任务        │                        │
└─────────────────────┴────────────────────────┘
```

### 窗口队列标签页

**适用场景**：同一浏览器执行多个任务，自动排队

**核心优势**：
- 浏览器连接复用，节省3-5秒/任务
- 任务自动排队，无需手动管理
- 套图自动归档，文件管理清晰

**使用说明**：
（详细功能开发中...）

### 批量队列标签页

**适用场景**：批量导入多个提示词，一次性执行

**操作流程**：
（详细功能开发中...）

---

## 主题切换

点击右上角 **🌙 深色** / **☀️ 浅色** 按钮可切换界面主题。

主题设置会自动保存到配置文件。

---

## 与V2版本的兼容性

### 配置文件兼容

V3使用相同的配置文件 `config_gui_v2.json`，所有配置可无缝迁移。

新增配置项：
```json
{
  "appearance_mode": "light"  // 界面主题：light 或 dark
}
```

### 后端兼容

V3保持与V2完全相同的后端逻辑：
- `whisk_core_v2.py` - 自动化核心（无修改）
- `whisk_window_queue.py` - 窗口队列管理（无修改）

### 并行使用

V2和V3可以并行使用：
- V2界面：`python whisk_launcher_v2.py` 或 `python whisk_gui_v2.py`
- V3界面：`python whisk_gui_v3.py` 或运行 `启动V3界面.bat`

---

## 快捷键（计划中）

| 快捷键 | 功能 |
|-------|------|
| Ctrl+N | 新建任务 |
| Ctrl+S | 停止选中任务 |
| Ctrl+L | 清除已完成 |
| F5 | 刷新浏览器列表 |
| Ctrl+T | 切换主题 |

---

## 已知问题和限制

### 当前版本（V3.0.0）

1. **窗口队列标签页** - 部分功能开发中
2. **批量队列标签页** - 部分功能开发中
3. **进度条可视化** - 暂时使用文字显示
4. **快捷键系统** - 尚未实现

### 后续计划

参见项目README中的"Phase 2"和"Phase 3"开发计划。

---

## 技术细节

### 依赖版本

```
customtkinter >= 5.2.0
requests >= 2.31.0
playwright >= 1.40.0
Pillow >= 10.0.0
```

### 文件结构

```
D:\whisk 更新版/
├── whisk_gui_v3.py          # V3主界面文件
├── whisk_gui_v2.py          # V2界面（保留）
├── whisk_core_v2.py         # 自动化核心（共用）
├── whisk_window_queue.py    # 窗口队列（共用）
├── gui/                     # GUI组件包
│   ├── __init__.py
│   ├── themes/              # 主题配置
│   │   ├── __init__.py
│   │   └── colors.py        # 颜色系统
│   ├── widgets/             # 自定义组件（计划中）
│   └── tabs/                # 标签页组件（计划中）
├── requirements_v2.txt      # 依赖声明
├── config_gui_v2.json       # 配置文件（共用）
└── 启动V3界面.bat           # 启动脚本
```

---

## 故障排除

### 问题1：CustomTkinter导入失败

**症状**：
```
ModuleNotFoundError: No module named 'customtkinter'
```

**解决方案**：
```bash
pip install customtkinter>=5.2.0
```

### 问题2：界面显示异常

**症状**：控件位置错乱、颜色不正常

**解决方案**：
1. 确保CustomTkinter版本 >= 5.2.0
2. 检查屏幕缩放设置（推荐100%）
3. 尝试切换主题（深色/浅色）

### 问题3：与V2界面冲突

**症状**：同时运行V2和V3时配置互相覆盖

**解决方案**：
- V2和V3共用同一配置文件，这是预期行为
- 如需独立配置，修改 `config_file` 路径

---

## 反馈和建议

如有问题或建议，请：
1. 查看项目文档目录中的相关说明
2. 提交Issue到项目仓库
3. 联系开发团队

---

**版本历史**：

- **V3.0.0** (2025-01-21) - 首次发布，基于CustomTkinter重构
- **V2.1** (2025-01) - 新增窗口队列功能
- **V2.0** (2024) - 基础版本

**祝使用愉快！** 🎉
