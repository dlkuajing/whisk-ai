# 窗口队列进度显示修复说明

## 🐛 问题描述

窗口队列功能正常运行，但任务列表中的进度和状态无法正常显示。

## 🔍 原因分析

在窗口队列模式下，任务被添加到WindowQueue后台执行，但GUI层面没有创建对应的TreeView显示项，导致：
- 任务运行时看不到进度
- 任务状态无法更新
- 用户无法了解任务执行情况

## ✅ 修复内容

### 1. 添加任务ID映射机制

**位置**: `whisk_gui_v2.py` Line 43

```python
self.queue_task_items = {}  # 窗口队列任务的TreeView项映射 {task_id: tree_item}
```

### 2. 在添加任务时创建TreeView项

**位置**: `add_task_to_queue()` 方法

**修改内容**:
```python
# 生成任务ID
self.thread_counter += 1
task_id = f"Q{self.thread_counter:03d}"

# 在task_params中添加task_id
task_params = {
    'task_id': task_id,  # 添加任务ID
    'task_name': self.task_name_var.get(),
    'prompt': prompt,
    ...
}

# 在TreeView中创建任务项
tree_item = self.task_tree.insert('', 'end', text=task_id, values=(
    browser_display.split(' ')[0],
    prompt[:30] + "..." if len(prompt) > 30 else prompt,
    self.ratio_var.get(),
    self.count_var.get(),
    "排队中",
    "等待中"
))

# 保存TreeView项的映射
self.queue_task_items[task_id] = {
    'tree_item': tree_item,
    'browser_id': browser_id,
    'browser_display': browser_display,
    'task_params': task_params
}
```

### 3. 增强回调事件处理

**位置**: `handle_queue_callback()` 方法

**修改内容**:
```python
# 任务开始时
elif event_type == 'task_start':
    task_id = data.get('task_id', '')
    # 更新TreeView状态
    self.message_queue.put(('queue_task_status', task_id, ('进度', '执行中', '状态', '运行中')))

# 任务完成时
elif event_type == 'task_complete':
    task_id = data.get('task_id', '')
    # 更新TreeView状态
    self.message_queue.put(('queue_task_status', task_id, ('进度', '已完成', '状态', '已完成')))

# 进度更新时
elif event_type == 'task_progress':
    task = data['task']
    task_id = task.get('task_id', '')
    current = data['current']
    total = data['total']
    progress_type = data['progress_type']

    # 更新进度显示
    if progress_type == 'download':
        progress_text = f"下载:{current}/{total}"
    elif progress_type == 'generation':
        progress_text = f"生成:{current}/{total}"

    self.message_queue.put(('queue_task_progress', task_id, progress_text))
```

### 4. 处理新的消息类型

**位置**: `process_messages()` 方法

**新增代码**:
```python
elif msg_type == 'queue_task_status':
    # 窗口队列任务状态更新
    if task_id in self.queue_task_items:
        tree_item = self.queue_task_items[task_id]['tree_item']
        # data 是一个元组: ('进度', value1, '状态', value2)
        try:
            self.task_tree.set(tree_item, data[0], data[1])
            if len(data) > 2:
                self.task_tree.set(tree_item, data[2], data[3])
        except:
            pass

elif msg_type == 'queue_task_progress':
    # 窗口队列任务进度更新
    if task_id in self.queue_task_items:
        tree_item = self.queue_task_items[task_id]['tree_item']
        try:
            self.task_tree.set(tree_item, '进度', data)
        except:
            pass
```

### 5. 批量队列同步修复

**位置**: `execute_batch_queue()` 方法

为批量队列中的每个任务也创建TreeView项和ID映射，确保批量任务的进度也能正常显示。

## 📊 修复效果

### 修复前
- ❌ 任务列表空白
- ❌ 看不到进度
- ❌ 不知道任务状态

### 修复后
- ✅ 任务添加后立即在列表中显示
- ✅ 实时显示任务进度（排队中 → 执行中 → 下载:X/Y → 已完成）
- ✅ 清晰的状态指示（等待中 → 运行中 → 已完成/失败）

## 🎯 任务状态流转

```
添加任务
  ↓
显示：排队中 | 等待中
  ↓
任务开始执行
  ↓
显示：执行中 | 运行中
  ↓
生成阶段
  ↓
显示：生成:1/4 | 运行中
  ↓
下载阶段
  ↓
显示：下载:3/8 | 运行中
  ↓
任务完成
  ↓
显示：已完成 | 已完成
```

## 📝 使用示例

### 单任务添加
```
1. 启用窗口队列模式
2. 添加任务A
   → 列表显示：Q001 | 浏览器1 | 提示词A | 1:1 | 4 | 排队中 | 等待中
3. 任务开始执行
   → 列表更新：Q001 | 浏览器1 | 提示词A | 1:1 | 4 | 执行中 | 运行中
4. 下载进度更新
   → 列表更新：Q001 | 浏览器1 | 提示词A | 1:1 | 4 | 下载:3/8 | 运行中
5. 任务完成
   → 列表更新：Q001 | 浏览器1 | 提示词A | 1:1 | 4 | 已完成 | 已完成
```

### 多任务队列
```
添加任务A → Q001 | 排队中 | 等待中
添加任务B → Q002 | 排队中 | 等待中
添加任务C → Q003 | 排队中 | 等待中

执行中：
Q001 | 下载:5/8 | 运行中  ← 正在执行
Q002 | 排队中 | 等待中    ← 等待执行
Q003 | 排队中 | 等待中    ← 等待执行
```

## 🔧 技术细节

### 任务ID生成
- 单任务：`Q001`, `Q002`, `Q003` ...
- 使用统一的计数器 `self.thread_counter`
- 确保ID唯一性

### 映射关系
```python
self.queue_task_items[task_id] = {
    'tree_item': tree_item,       # TreeView项引用
    'browser_id': browser_id,     # 浏览器ID
    'browser_display': browser_display,  # 浏览器显示名
    'task_params': task_params    # 任务参数
}
```

### 消息流转
```
WindowQueue (后台线程)
    ↓ 回调
handle_queue_callback()
    ↓ 消息队列
process_messages()
    ↓ UI更新
TreeView显示
```

## ✅ 测试验证

### 测试步骤
1. 启动程序
2. 启用窗口队列模式
3. 添加任务
4. 观察任务列表
5. 验证进度更新
6. 验证状态变化

### 预期结果
- ✅ 任务立即显示在列表中
- ✅ 进度实时更新
- ✅ 状态正确显示
- ✅ 完成后状态为"已完成"

## 📋 相关文件

- `whisk_gui_v2.py` - GUI主文件（已修复）
- `whisk_window_queue.py` - 窗口队列核心（无需修改）

## 🎉 修复完成

现在窗口队列功能完全正常，包括：
1. ✅ 任务正常执行
2. ✅ 进度实时显示
3. ✅ 状态正确更新
4. ✅ 批量任务支持

**可以正常使用窗口队列功能了！**
