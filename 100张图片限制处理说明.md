# 100张图片限制自动处理功能

## 问题描述

Google Whisk AI 在生成约100张图片后会自动跳转到以下页面：
1. 错误页面：显示 "SORRY, WE CAN'T SEEM TO FIND THIS CONTENT"
2. 需要点击 "BROWSE GALLERY" 按钮进入Gallery页面
3. 在Gallery页面需要点击 "进入工具" 按钮才能回到生成页面

## 解决方案 V2.3 (增强版)

已在 `whisk_core_v2.py` 中实现增强的自动检测和恢复机制。

### 新增功能

#### 1. 增强的页面状态检测 (`check_and_handle_limit_page`)
- 自动检测限制页面的多种情况
- 识别页面类型：错误页面(error)和Gallery页面(gallery)
- 多重恢复策略，确保任务可以继续

#### 2. 多重恢复策略
- **策略1**：错误页面 → 点击 BROWSE GALLERY 按钮
- **策略2**：Gallery页面 → JavaScript自动点击项目卡片或"进入工具"按钮
- **策略3**：创建新项目（导航到 /whisk/new）
- **策略4**：强制导航到项目页面（多个备用URL）

#### 3. 增强的输入提示词功能 (`input_prompt`)
- 3次重试机制，确保输入成功
- 多种选择器查找输入框
- JavaScript和传统输入方法双重保障
- 自动恢复机制：检测限制页面、直接导航、页面刷新
- 详细的调试信息输出

#### 4. 生成计数器机制
- 在 `generate_images` 方法中添加生成计数器
- 每生成约45次（90张图片）预先检查页面状态
- 处理限制后自动重置计数器

### 集成位置

1. **输入提示词前** (`input_prompt`)
   - 检查并处理限制页面
   - 确保输入框可用

2. **生成循环中** (`generate_images`)
   - 监控生成次数
   - 接近限制时主动检查
   - 自动恢复并继续任务

3. **任务队列执行** (`run_queue`)
   - 任务切换时检查页面状态
   - 确保每个任务都能正常开始

## 技术细节

### 页面元素识别

**错误页面特征：**
- 文本包含 "SORRY" 和 "CAN'T SEEM TO"
- 存在 "BROWSE GALLERY" 按钮

**Gallery页面特征：**
- URL包含 "/fx/tools/whisk" 但不包含 "project"
- 没有可见的textarea输入框
- 存在 "进入工具" 按钮或项目卡片

### 选择器策略

**Browse Gallery按钮：**
```javascript
'a:has-text("BROWSE GALLERY")'
'button:has-text("BROWSE GALLERY")'
'a[href="/fx/tools/whisk"]'
'*:has-text("BROWSE GALLERY")'
```

**输入框选择器（按优先级）：**
```javascript
'textarea[placeholder*="prompt"]'
'textarea:visible'
'textarea[placeholder*="Enter"]'
'textarea[placeholder*="Type"]'
'textarea'
```

**JavaScript自动恢复：**
```javascript
// 优先查找"进入工具"按钮
// 其次查找项目卡片 div[role="button"]
// 最后查找任何可点击的项目元素
```

## 使用效果

- ✅ **无需人工干预**：自动处理100张限制
- ✅ **任务连续性**：不会中断批量任务执行
- ✅ **智能恢复**：自动重置状态继续生成
- ✅ **预防性检测**：接近限制时提前检查

## 注意事项

1. **计数器策略**：基于生成次数而非实际图片数，因为有时生成可能失败
2. **等待时间**：页面跳转后需要适当等待加载
3. **URL记录清理**：恢复后清空已记录的图片URL，避免混淆

## 未来优化

- 可以记录实际生成的图片总数，更精确地预测限制
- 添加配置选项，允许用户调整检查频率
- 收集更多页面变化模式，提高识别准确性
- 使用机器学习识别页面状态变化模式

## 更新记录

- **V2.3** (2025-09-29): 增强恢复机制，添加多重策略和JavaScript自动化
- **V2.2** (2024-09-28): 初始版本，基础限制检测和恢复

---
**更新日期**: 2025-09-29
**版本**: V2.3