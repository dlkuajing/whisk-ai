# 批量任务队列功能说明

## 功能概述

新增的批量任务队列功能允许你在单个浏览器窗口中串行执行多个不同的生成任务，每个任务可以有不同的提示词、生成数量和纵横比设置。系统会自动在任务之间刷新页面，确保稳定执行。

## 主要特性

1. **串行执行**：在同一个浏览器窗口中按顺序执行多个任务
2. **自动刷新**：每个任务完成后自动刷新页面，避免累积问题
3. **独立配置**：每个任务可以有独立的提示词、数量和纵横比
4. **批量导入**：支持批量导入多个提示词快速创建任务
5. **任务管理**：可以调整任务顺序、编辑或删除队列中的任务
6. **保存/加载**：支持保存和加载任务队列配置

## 使用方法

### 1. 打开批量任务队列

在主界面点击"批量任务队列"按钮，打开任务队列管理窗口。

### 2. 添加单个任务

1. 在左侧"添加任务"区域填写：
   - 任务名称
   - 提示词
   - 纵横比
   - 生成数量
2. 点击"添加到队列"按钮

### 3. 批量添加任务

1. 在"批量导入提示词"区域，每行输入一个提示词
2. 设置统一的纵横比和生成数量
3. 点击"批量添加"按钮

### 4. 管理队列

- **上移/下移**：调整任务执行顺序
- **编辑**：修改已添加的任务
- **删除**：移除不需要的任务
- **清空队列**：清除所有任务

### 5. 执行队列

1. 选择执行浏览器（确保浏览器已打开并在Whisk页面）
2. 设置保存目录
3. 选择是否为每个任务创建独立文件夹
4. 点击"开始执行队列"

## 执行流程

1. 系统连接到指定的浏览器窗口
2. 执行第一个任务：
   - 输入提示词
   - 选择纵横比
   - 生成图片
   - 下载图片到指定目录
3. 任务完成后，自动刷新页面
4. 继续执行下一个任务
5. 重复直到所有任务完成

## 文件组织

如果选择"为每个任务创建独立文件夹"，目录结构如下：

```
downloads/
└── batch_queue_20240101_120000/
    ├── 01_任务1/
    │   ├── whisk_20240101_120100_1_左侧.jpg
    │   └── whisk_20240101_120100_2_右侧.jpg
    ├── 02_任务2/
    │   ├── whisk_20240101_120200_3_左侧.jpg
    │   └── whisk_20240101_120200_4_右侧.jpg
    └── 03_任务3/
        └── ...
```

## 注意事项

1. **浏览器状态**：执行前确保浏览器已打开并在Whisk页面
2. **网络稳定**：确保网络连接稳定，避免中断
3. **任务数量**：建议单次队列不超过20个任务，避免长时间运行
4. **监控执行**：执行过程中可以在主界面查看日志和进度
5. **停止任务**：如需中断，可以使用"停止所有"按钮

## 最佳实践

1. **测试运行**：首次使用时先测试1-2个任务
2. **保存队列**：配置好的队列可以保存为JSON文件，方便重复使用
3. **合理间隔**：保持默认的延迟设置，避免请求过于频繁
4. **定期检查**：长时间运行时定期检查执行状态

## 故障排除

- **连接失败**：检查比特浏览器是否正常运行
- **生成失败**：检查页面是否正确加载，网络是否正常
- **下载失败**：检查保存目录权限和磁盘空间
- **页面异常**：任务会自动尝试刷新页面恢复