# WhiskAI V2 窗口队列功能实现总结

## 📦 新增文件

### 1. `whisk_window_queue.py` (新文件)
核心窗口队列管理模块，包含3个核心类：

#### `TaskSession`
- 管理任务组（套图）
- 处理文件归档结构
- 跟踪任务组状态和空闲时间

#### `WindowTaskQueue`
- 单个浏览器窗口的任务队列
- 维护持久化浏览器连接
- 工作线程自动处理队列任务
- 支持独立任务和任务组模式

#### `WindowQueueManager`
- 全局管理所有窗口队列
- 提供统一的任务添加接口
- 处理队列状态查询和控制

---

## 🔧 修改文件

### 1. `whisk_core_v2.py`

**修改点**:
```python
# Line 86-95: 增强 reset_task_state()
def reset_task_state(self):
    """重置任务状态"""
    with self.lock:
        self.should_stop = False
        self.is_running = False
        self.downloaded_count = 0
        self.total_images_expected = 0
        # 新增：清空URL去重集合，确保任务间完全隔离
        self.downloaded_urls.clear()
        self.pre_generation_urls.clear()
```

**作用**: 确保队列任务间状态完全隔离，避免图片去重问题

---

### 2. `whisk_gui_v2.py`

#### 核心架构变更

**新增导入** (Line 21):
```python
from whisk_window_queue import WindowQueueManager
```

**新增实例变量** (Line 42):
```python
self.window_queue_manager = None  # 窗口队列管理器
```

**新增配置项** (Line 71-74):
```python
"use_window_queue": False,      # 是否启用窗口队列模式
"session_mode": "ask",          # 任务归档模式
"idle_timeout": 300,            # 队列空闲超时（秒）
"session_timeout": 600          # 任务组自动结束超时（秒）
```

#### 新增UI组件

**窗口队列模式开关** (Line 287-293):
```python
# 启用窗口队列模式
self.use_window_queue_var = tk.BooleanVar(value=self.config.get('use_window_queue', False))
queue_cb = ttk.Checkbutton(config_frame, text="启用窗口队列模式（同窗口任务自动串行）",
                           variable=self.use_window_queue_var,
                           command=self.toggle_queue_options)
```

**归档模式选择** (Line 295-308):
```python
# 任务归档模式
self.archive_frame = ttk.Frame(config_frame)
# 三个单选按钮：
# - 每次询问
# - 总是独立
# - 总是套图
```

#### 新增方法

**1. `init_window_queue_manager()` (Line 96-109)**
- 初始化WindowQueueManager
- 配置回调函数和参数

**2. `handle_queue_callback()` (Line 111-150)**
- 处理窗口队列的所有事件回调
- 转换为消息队列消息，更新UI

**3. `toggle_queue_options()` (Line 534-543)**
- 控制归档模式选项的启用/禁用

**4. `add_task()` (Line 558-597) - 重构**
```python
def add_task(self):
    # 验证输入...
    # 保存配置...

    # 判断使用哪种模式
    if self.use_window_queue_var.get():
        # 窗口队列模式
        self.add_task_to_queue(browser_id, browser_display, prompt)
    else:
        # 传统独立任务模式
        self.add_independent_task(browser_id, browser_display, prompt)
```

**5. `add_task_to_queue()` (Line 599-660) - 新增**
- 窗口队列模式的任务添加逻辑
- 处理归档模式选择
- 调用WindowQueueManager添加任务

**6. `add_independent_task()` (Line 804-...) - 新增**
- 传统独立任务模式（保持向后兼容）
- 原有add_task的核心逻辑

**7. `ask_session_choice()` (Line 662-711) - 新增**
- 询问用户是否加入现有任务组
- 显示当前任务组信息

**8. `ask_first_task_choice()` (Line 713-754) - 新增**
- 第一个任务时询问归档方式
- 创建新套图或独立文件夹

**9. `ask_session_name()` (Line 756-802) - 新增**
- 询问任务组名称
- 提供默认名称

**10. `execute_batch_queue()` (Line 1107-1156) - 重构**
```python
def execute_batch_queue(self, queue_info):
    # 询问任务组名称
    session_name = self.ask_session_name()

    # 批量添加所有任务到窗口队列
    for idx, task in enumerate(task_queue, 1):
        # 第一个任务创建新任务组
        if first_task:
            session_mode = 'new_session'
        else:
            session_mode = 'join_current'

        # 使用WindowQueueManager添加
        self.window_queue_manager.add_task_to_window(...)
```

---

### 3. `whisk_queue_dialog.py`

**无需修改**
- 批量队列对话框保持不变
- 仍然用于配置多个任务
- 返回的结果由GUI的`execute_batch_queue()`处理

---

## 🎯 核心工作流

### 窗口队列模式工作流

```
用户添加任务A
    ↓
GUI: add_task() → add_task_to_queue()
    ↓
询问归档模式（如果是"每次询问"）
    ↓
WindowQueueManager.add_task_to_window()
    ↓
WindowTaskQueue.add_task()
    ↓
任务加入Queue
    ↓
WorkerThread自动获取并执行
    ↓
使用WhiskAutomationCoreV2执行
    ↓
完成后继续执行队列中下一个任务
```

### 任务间隔离机制

```
任务A执行完成
    ↓
调用 automation.reopen_page()
    ↓
清空 downloaded_urls 和 pre_generation_urls
    ↓
调用 automation.reset_task_state()
    ↓
进一步清空状态变量
    ↓
任务B开始执行（状态完全隔离）
```

---

## 📂 文件归档机制

### 独立任务模式
```python
downloads/
 ├─ 任务A_20250121_100000/
 ├─ 任务B_20250121_100500/
 └─ 任务C_20250121_101000/
```

### 套图模式（任务组）
```python
downloads/
 └─ 赛博朋克系列_20250121_100000/    # TaskSession创建
     ├─ 01_城市夜景/                 # 任务A子文件夹
     ├─ 02_人物肖像/                 # 任务B子文件夹
     └─ 03_科技元素/                 # 任务C子文件夹
```

### 混合模式
```python
downloads/
 ├─ 风景系列_xxx/              # 任务组1
 │   ├─ 01_山景/
 │   └─ 02_湖景/
 ├─ 独立任务_xxx/              # 独立任务
 └─ 人物系列_xxx/              # 任务组2
     └─ 01_肖像/
```

---

## 🔄 向后兼容性

### 保持兼容的设计
1. **默认禁用**: `use_window_queue` 默认为 False
2. **独立代码路径**: 传统模式使用 `add_independent_task()`
3. **配置独立**: 窗口队列配置不影响传统模式
4. **UI可选**: 归档模式选项仅在启用窗口队列时显示

### 用户迁移路径
```
传统用户
    ↓
不勾选"启用窗口队列" → 完全不受影响
    ↓
想体验新功能 → 勾选开关
    ↓
选择"总是独立" → 类似传统模式，但有队列能力
    ↓
选择"总是套图" → 自动套图模式
    ↓
选择"每次询问" → 灵活控制（推荐）
```

---

## 📊 代码统计

| 文件 | 新增行数 | 修改行数 | 核心类/方法 |
|------|---------|---------|-----------|
| `whisk_window_queue.py` | ~550 | 0 | 3个类 |
| `whisk_core_v2.py` | 2 | 1 | reset_task_state() |
| `whisk_gui_v2.py` | ~400 | ~100 | 10个新方法 |
| **总计** | **~950** | **~100** | - |

---

## ✅ 实现的核心功能

1. ✅ 窗口任务队列基础设施
2. ✅ 任务组（套图）管理
3. ✅ 动态任务添加
4. ✅ 连接保持和复用
5. ✅ 灵活的归档模式
6. ✅ 批量队列集成
7. ✅ 询问对话框
8. ✅ 向后兼容
9. ✅ 空闲超时机制
10. ✅ 任务间状态隔离

---

## 🚀 技术亮点

### 1. 线程安全设计
- 使用 `queue.Queue` 保证线程安全
- 使用 `threading.Lock` 保护共享状态
- 消息队列模式解耦GUI和工作线程

### 2. 连接复用优化
- 单个窗口只建立一次浏览器连接
- 任务间使用 `reopen_page()` 重置状态
- 空闲超时自动断开，节省资源

### 3. 灵活的归档系统
- 支持独立任务和任务组混合
- 用户可控的归档策略
- 清晰的文件组织结构

### 4. 渐进式集成
- 不破坏现有功能
- 可选启用新功能
- 平滑的用户体验

---

## 🔮 未来扩展方向

1. **分组TreeView**: 任务列表支持按窗口分组显示
2. **队列管理面板**: 可视化管理所有窗口队列
3. **任务模板**: 保存常用配置快速应用
4. **智能分配**: 自动选择负载最轻的窗口
5. **队列持久化**: 保存队列状态，程序重启后恢复
6. **进度预估**: 显示预计完成时间
7. **批量操作**: 支持拖拽排序、批量移除等

---

## 📝 总结

本次实现成功将窗口串行队列功能集成到现有系统中，在保持向后兼容的同时，大幅提升了工作效率。核心设计遵循以下原则：

1. **渐进式增强**: 不破坏现有功能，可选启用
2. **用户友好**: 清晰的UI和提示，降低学习成本
3. **架构清晰**: 模块化设计，易于维护和扩展
4. **功能完整**: 覆盖独立任务、套图、混合等所有场景

用户可以根据自己的工作流选择最适合的模式，真正做到"既能快速测试，也能批量生产"。
